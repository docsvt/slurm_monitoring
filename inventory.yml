---
all:
  hosts:
    prometheus:
      ansible_host: "192.168.200.61"
  children:
    monitoring:
      hosts:
        prometheus:
    prometheus:
      hosts:
        prometheus:
    node_exporter:
      hosts:
        prometheus:
    elk:
      hosts:
        prometheus:
    fluentbit:
      hosts:
        prometheus:
